# cloq-agent — SPEC

> **Moved.** The project spec now lives at [`docs/SPEC.md`](docs/SPEC.md) as the single source of
> truth (this file used to be a duplicate copy and would drift). See also:
>
> - [`docs/RESULTS.md`](docs/RESULTS.md) — what works today and the precise ceiling
> - [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md) — the reuse map and the loop
> - [`README.md`](README.md) — quickstart, GUI/API, CI
>
> **Status reminder:** the FPGA hardware-oracle track is **deferred / parked** (off the critical
> path); output is proof-only (closed form + predicted range). CI runs on GitHub Actions with a
> mirrored GitLab pipeline (`.gitlab-ci.yml`).
