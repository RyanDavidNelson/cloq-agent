# Vendored Picinae/Cloq is PRE-BUILT in the image. Use -R for the core logical
# root and -I (physical search path) for the dirs holding bare-Required files,
# so `Require Import NEORV32 / array / riscv_addloop_timing_proof` resolve by
# filename without logical-path collisions.
-R ../vendor/picinae Picinae
-I ../vendor/picinae/timing/riscv
-I ../vendor/picinae/timing/examples/data_structures/array/find_in_array
-I ../vendor/picinae/timing/examples
-I ../vendor/picinae/timing/examples/crypto/ct_swap
-I ../vendor/picinae/timing/examples/FreeRTOS
-I ../vendor/picinae/timing/examples/FreeRTOS/list
# C-intake: generated program scaffolding (<func>_lifted.vo) is written + coqc'd here, so a
# generated theorem can `Require Import <func>_lifted`. A recursive -R mapping (not -I/-Q) is
# required: bare Requires resolve by suffix-matching a logical path under an -R root, and
# `targets/` is not otherwise under the vendored -R root.
-R targets Targets

-arg -w -arg -notation-overridden
-arg -w -arg -deprecated-since-8.20
-arg -w -arg -overriding-logical-loadpath

targets/Addloop.v
